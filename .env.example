# Database URL used by Prisma.
# For local dev, use the bundled SQLite file.
# For production (Vercel / Fly / etc.) point this at Postgres, Turso, or another
# managed provider, and update prisma/schema.prisma's `provider` accordingly.
DATABASE_URL="file:./stratum.db"
